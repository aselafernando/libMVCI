# NOTE: git does not honour trailing "# ..." comments on a pattern line — the
# text becomes part of the pattern. Keep every comment on its own line.

# ---- build output ----
# CMake build tree
/build/
# VS "Open Folder" default CMake build tree
/out/
# stray artifacts if built in-tree
/lib/
/mvci_test
*.o
*.obj
*.so
*.dll
*.dylib
*.exe
*.lib
*.exp
*.pdb
*.ilk
CMakeUserPresets.json

# ---- editor / IDE cache (never commit) ----
.vs/
*.user

# ---- reverse-engineering material (kept out of the public repo) ----
/reverse_engineering/
